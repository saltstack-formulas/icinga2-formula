{% set icinga2 = salt['grains.filter_by']({
    'Debian': {
        'classicui': {
            'users': {}
        },
        'conf': {},
        'nrpe': {
            config: {},
        },
        'pkgs': []
    },

}, merge=salt['pillar.get']('icinga2:lookup')) %}
